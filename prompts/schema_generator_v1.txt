你是视觉数据协议设计器。根据类别聚合结果和代表性目标图，自动设计一份可执行的视觉特征 schema。

只允许定义可从当前图像直接观察到的外观、姿态、动作、服饰、交互、场景、遮挡和截断特征。
禁止定义年龄、性别、表观性别、种族、民族、肤色类别、身份、姓名、职业、健康、残疾、情绪、性格、吸引力、收入、宗教或人物匹配等属性。
每个特征必须是有限 enum、有限 set、integer 或 number；enum/set 必须包含 unknown；所有字段必须 allow_unknown=true 且 evidence_required=true。
feature_id 使用 snake_case，不能重复。不要定义自由文本特征。rules 必须是 JSON 数组；不需要依赖关系时输出空数组。
字段数量必须在固定元规则范围内。person 类别应覆盖可见性、朝向、姿态、动作/交互、可观察服装、场景、遮挡和截断，但不能为了凑数量加入敏感属性。

只输出一个 JSON 对象，不输出 Markdown 或解释文字：
{"schema_version":"proposal","category":"person","features":[{"feature_id":"body_orientation","group":"orientation","value_type":"enum","possible_values":["front","back","unknown"],"allow_unknown":true,"evidence_required":true}],"rules":[]}
